# Drop your raster illustrations here. See RASTER_ASSETS.md at the project
# root for filenames and dimensions per density bucket.
#
# This file exists only so the empty folder is committed to git. Delete
# it once you've added real PNGs (or leave it; it's harmless).
